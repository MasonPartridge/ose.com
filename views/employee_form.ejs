<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form method="post" action="">
        <label for="first_name">First Name</label>
        <input 
            id="first_name"
            type="text"
            required="true"
            name="first_name_field"
        />
        <label for="family_name">Family Name</label>
        <input 
            id="family_name"
            type="text"
            name="family_name_field"
        />
        <label for="date_of_birth">Date of Birth:</label>
        <input 
            id="date_of_birth"
            type="date"
            name="date_of_birth_field"
        />
        <label for="date_of_death">Date of Death:</label>
        <input 
            id="date_of_death"
            type="date"
            name="date_of_death_field"
        />
        <label for="clearance_level">Clearance Level:</label>
        <select 
            for="clearance_level"
            id="clearance_level"
            name="clearance_level_field"
            required="true"
        >
        <% ["I", "II", "III", "IV", "V"].forEach((clearance_level) => { %>
            <option value="<%= clearance_level %>"><%= clearance_level %></option>
        <% }); %>
        </select>
        <input type="submit" value="Submit Employee Form" />
    </form>
</body>
</html>